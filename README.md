# Web-blog-rails

Practising rails with a simple blog.

## Installation
In the terminal run:
```
bundle install
bin/rails db:create
bin/rails db:migrate
```

Start the server at localhost:3000
```
bin/rails server
```